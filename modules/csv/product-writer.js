// dependencies

// exports

module.exports = {
    init: _init
};

// initialization

function _init(csvFilePath) {
    var filePath = csvFilePath;

    return {
        addProduct: _addProduct,
        addProducts: _addProducts
    };

    // private methods

    function _addProduct(product) {

    }

    function _addProducts(products) {

    }
}